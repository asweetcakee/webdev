{
  "name": "phonebook",
  "version": "0.0.1",
  "description": "contains all part3 exercises of Full Stack Open course",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "\"echo \"Error: no test specified\" && exit 1\"",
    "build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend"
  },
  "author": "asweetcakee",
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.0"
  }
}
